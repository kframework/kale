package org.kframework.kale.builtin

import org.kframework.kale.Environment
import org.kframework.kale.builtin

case class Hooks(implicit env: Environment) {

//  val BOOLEAN = builtin.BOOLEAN()
//  val INT = builtin.INT()(env)

//  val allModules = Set(INT, BOOLEAN)

//  val apply: Map[String, Module] = allModules map (m => (m.moduleName, m)) toMap
}
